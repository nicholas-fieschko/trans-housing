Hi, #{@receiver.name}!

You have a new message from #{@sender.name}.

***
#{@message.text}
***

Click to
= link_to("reply", user_conversation_url(user_id: @receiver.id.to_s, id: @thread.to_s))

Visit your
= link_to("account", user_url(id: @receiver.id.to_s))