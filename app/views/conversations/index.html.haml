.pure-g.grid-columns-container
  .pure-u-1-5
  .pure-u-3-5.login-box-positioner
    .sign-up-box.sign-up-column
      %h1 Conversations
      %ul
      - @inbox.each do |c|
        - if !c.deleted_conversation?(current_user)
          %li
          - if c.unread_conversation?(current_user)
            = "[UNREAD] " 
          = c.print_owners
          = c.subject.to_s
          = link_to 'View >>', user_conversation_path(id: c.id)
      - if @inbox.empty?
        Your inbox is empty.
        -#=  link_to 'Send a new message', new_user_conversation_path (check if trashed or not!)