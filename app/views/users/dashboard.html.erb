<h1> Hi <%= @user.name %></h1>

<% if @reviews.any? %>

	Reminder: please complete the following reviews: <br>

	<% @reviews.each do |r| %>
		Review for <%= User.find(r.user_id).name %>: <%= link_to "Click here", edit_user_review_url(r.user_id.to_s, r.id.to_s) %><br>
	<% end %>


<% else %>


	All reviews completed.


<% end %>






<br><br>




<h3> History </h3>

<% @user.requests.each do |r| %>
	<% if @user.is_provider %>
		<% if r.completed %>
			Helped <%= User.find(r.seeker).name %> <%= time_ago_in_words(r.updated_at) %> ago. <br>
		<% else %>
			Remember to <%= link_to "finish helping #{User.find(r.seeker).name}", user_request_path(User.find(r.seeker), r) %>. <br>
		<% end %>
	<% else %>
		<% if r.completed %>
			Helped by <%= User.find(r.provider).name %> <%= time_ago_in_words(r.updated_at) %> ago. <br>
		<% else %>
			Reminder: You have a <%= link_to "help request", user_request_path(User.find(r.provider), r) %> with <%= User.find(r.provider).name %>. <br>
		<% end %>
	<% end %>
<% end %>